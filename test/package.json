{
    "name": "hitster",
    "version": "1.0.0",
    "description": "Norwegian music quiz PWA — players place songs chronologically on a timeline",
    "private": true,
    "scripts": {
        "test": "node test.js",
        "lint": "eslint *.js",
        "format": "prettier --write '*.js' '*.css' '*.html'",
        "format:check": "prettier --check '*.js' '*.css' '*.html'"
    },
    "devDependencies": {
        "eslint": "^9.0.0",
        "prettier": "^3.0.0"
    }
}
